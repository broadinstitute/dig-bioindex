pytest==9.0.3
pytest-asyncio==1.3.0
httpx==0.27.0

# Note: bash scripts under scripts/ are tested with bats-core, which
# is NOT pip-installable. Install separately: `brew install bats-core`
# or `apt install bats`. Tests live in tests/scripts/.
